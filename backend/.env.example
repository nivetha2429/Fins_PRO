# IMPORTANT: Your MongoDB connection string needs the database name!

# ❌ INCORRECT (what you currently have):
# MONGODB_URI=mongodb+srv://nivetha:1402@cluster0.irgkzqa.mongodb.net/?retryWrites=true&w=majority&appName=Fins_PRO

# ✅ CORRECT (add /fins_pro before the ?):
MONGODB_URI=mongodb+srv://nivetha:1402@cluster0.irgkzqa.mongodb.net/fins_pro?retryWrites=true&w=majority&appName=Fins_PRO

# Other required environment variables:
PORT=5000
NODE_ENV=development
JWT_SECRET=fins_pro_secret_key_2026_change_in_production

# INSTRUCTIONS:
# 1. Copy the CORRECT connection string above (the one with /fins_pro)
# 2. Replace the MONGODB_URI line in your backend/.env file
# 3. Save the file
# 4. The server will automatically restart and connect to the fins_pro database
