import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.securefinance.emilock.superadmin',
  appName: 'Fins Pro Admin',
  webDir: 'dist'
};

export default config;
